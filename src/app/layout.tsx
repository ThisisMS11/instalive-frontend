import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/providers";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "InstaLive - Professional YouTube Live Streaming",
  description:
    "Stream to YouTube Live with overlays, real-time chat moderation, viewer analytics, and AI-powered spam detection.",
  keywords: [
    "YouTube Live",
    "streaming",
    "live stream",
    "overlays",
    "chat moderation",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} font-sans antialiased`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
